<% if @users.blank? %>
  <h4>There are no users containing the term <%= params[:search] %></h4>
<% else %>
  <ul>
    <% @users.each do |user| %>
      <li><%= link_to user.username, user %></li>
    <% end %>
  </ul>
<% end %>
